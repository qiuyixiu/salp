<nav class="navbar navbar-default navbar-fixed-top navbar-style" role="navigation">
  <div class="container">
    <div class="navbar-header nav-container">
      <img src="http://oon3erbcp.bkt.clouddn.com/%E6%97%8B%E8%BD%AC%E5%88%806.png">
      <a href="/" class="navbar-brand " style="font-size: 1.5em;">深圳市皮雕艺术促进会</a>

      <!-- 响应式导航 -->
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- 菜单 -->
    <div class="navbar-collapse navbar-ex1-collapse collapse navbar-right" aria-expanded="false">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to("了解促进会", introductions_path) %>
        </li>
        <li>
          <%= link_to("通知公告", notices_path) %>
        </li>
        <li>
          <%= link_to("动态", messages_path) %>
        </li>
        <li>
          <%= link_to("皮雕大赛", games_path) %>
        </li>
        <% if !current_user %>
          <li><%= link_to("注册", new_user_registration_path) %></li>
          <li><%= link_to(content_tag(:i, "登录", class: "fa fa-sign-in"), new_user_session_path) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              你好，<img width="30" height="30" src= "<%= render_user_avatar(current_user, 20) %>" class="avatar"> <%= current_user.name %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <% if current_user.admin? %>
                <li><%= link_to("管理后台", admin_introductions_path) %></li>
              <% end %>
              <li><%= link_to("个人中心", edit_account_user_path(current_user)) %></li>
               <li><%= link_to("修改密码", edit_registration_path(:user)) %></li>
              <li><%= link_to(content_tag(:i, "退出", class: "fa fa-sign-out"), destroy_user_session_path, method: :delete) %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
